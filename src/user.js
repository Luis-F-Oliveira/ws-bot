class User
{
    constructor(token, id, name, email, sector)
    {
        this.token = token,
        this.id = id,
        this.name = name,
        this.email = email,
        this.sector = sector
    }
}

module.exports = User